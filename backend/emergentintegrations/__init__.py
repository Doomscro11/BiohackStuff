"""Local compatibility namespace for legacy LLM imports.

This package exists only to keep older imports stable while the application
uses the official OpenAI SDK instead of the external Emergent integration
package.
"""
